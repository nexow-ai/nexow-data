[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uv run uvicorn nexow_data.api:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[env]
UV_SYSTEM_PYTHON = "1"
